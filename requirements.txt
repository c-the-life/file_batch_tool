# 文件批量处理工具 - 依赖列表
# 安装方法：pip install -r requirements.txt
#
# 注意：
# - 核心依赖是必须安装的
# - AI支持是可选的，如需使用真实AI引擎请安装 openai
# - 开发测试依赖是可选的，用于开发和测试

# 核心依赖
Pillow>=10.0.0       # 图片处理：格式转换、水印、EXIF提取
PyQt5>=5.15.0        # GUI界面：图形化用户界面

# AI支持（可选）
openai>=1.0.0        # OpenAI API：使用真实AI引擎解析自然语言命令

# 可选依赖（开发测试用）
# pytest>=7.0.0       # 单元测试
# black>=23.0.0      # 代码格式化
